# 开发日志

## 2026-07-19：项目骨架与全栈 MVP

- 保存可编译的 HarmonyOS NEXT API 24 空白工程基线，并建立 Git 仓库。
- 建立唯一的 `Navigation + NavPathStack + PageMap`，完成登录注册、首页、学生档案、AI 助手、课程、试卷、学习计划和个人中心基础页面。
- 增加网络和麦克风权限，接入语音识别、TTS、图片选择与 OCR 降级流程。
- 创建 FastAPI、JWT、家庭与学生数据隔离、课程/试卷/学习任务 CRUD，以及前端统一 HTTP 服务层。
- 准备 12 门课程和 24 份试卷样例，完成课程分层规则和基础多轮会话。

## 2026-07-19：MySQL 切换

- 将开发数据库由 SQLite 切换为 MySQL 8.0.45，SQLite 仅保留给自动化测试。
- 创建受限应用账号、数据库初始化脚本和 `.env.example`，避免在仓库中保存密码。
- 完成 Alembic 初始迁移、种子数据和注册登录接口冒烟测试。
- 处理 SQLAlchemy 与 asyncmy 连接池兼容问题，使用连接回收配置避免长期空闲连接失效。

## 2026-07-20：个人中心完善

- 将“我的”页面重构为可滚动教育平台个人中心，保留用户、角色、家庭邀请码、学生绑定码和退出登录。
- 新增学习概览、我的课程、订单状态入口、常用功能宫格、家庭学生卡片和其他服务列表。
- 新增我的课程、我的订单、我的收藏、客服中心、客服聊天、意见反馈、设置、错题本、学习报告和平台信息页面。
- 新增个人中心模型、集中式 Mock 数据和 Service 降级层；接口未实现或后端关闭时不白屏。
- 订单筛选使用明确路由参数；所有子页面沿用现有 NavPathStack，不重复显示底部导航。

## 2026-07-20：首页重构与在线练习

- 将原纵向大卡片首页改为欢迎区域、三页 Swiper、四项快捷入口、学生概览、AI 推荐、热门课程、最新课程、推荐试卷和今日任务。
- 拆分 10 个首页组件，集中维护首页模型与演示数据，避免将业务数据散落在 UI 文件中。
- 新增 `HomeService`，页面立即渲染 Mock，再尝试请求 `/api/home`；网络失败时保留可用内容。
- 新增 `OnlinePracticePage`，支持真实题目优先、本地题目兜底、单选、前后切题、提交、得分和解析。
- 所有课程和试卷卡片使用真实 Mock 数据 ID，确保详情与答题路由可用。

## 2026-07-20：AI 全链路完善

- 接入 DeepSeek/Mock Provider、13 类意图、8 个业务工具、RAG、会话摘要和结构化卡片。
- 新增 `0002_ai_full_stack` 增量迁移，覆盖订单、报名、题目、练习、错题、RAG、AI 请求与工具日志扩展。
- 完成 `/api/home`、`/api/ai/*`、订单、真实试题、练习提交、错题与学习报告接口。
- 课程推荐统一使用可追踪规则；课程/试卷/订单 ID、价格、成绩和状态只取自数据库或工具结果。
- SSE 改为真实事件流，并通过 `clientMessageId` 保证首段失败降级普通接口时不重复创建业务数据。

## 2026-07-20：AI 学习顾问界面重构

- 将 cards、sources、流式状态和错误状态绑定到具体 AI 消息，不再使用页面级临时结果区。
- 新增双方头像、快捷问题、输入栏、打字状态、来源折叠区及课程/试卷/计划/订单专用卡片。
- 增加纯 ArkTS 轻量 Markdown 解析与渲染，支持标题、段落、列表、分隔线和 `**粗体**`。
- SSE 分片持续更新同一个 AI 气泡；首段失败自动降级，中途失败保留已生成文本并允许重试。
- 会话 ID 使用 Preferences 持久化，支持历史恢复、新会话、清空确认、单条回复朗读和页面退出资源清理。
- 未绑定学生时允许普通教育咨询；课程推荐、学习计划和报告等个性化能力会引导创建或绑定学生档案。

## 2026-07-20：测试与 GitHub 交付

- 后端完整测试更新为 17 项并全部通过；测试强制 SQLite + Mock Provider，不读取真实 DeepSeek Key。
- HarmonyOS API 24 `assembleHap` 构建成功，无新增 ArkTS 编译错误。
- 完成仓库密钥扫描，未提交真实 DeepSeek Key、MySQL 密码或 `server/.env`。
- AI 全栈功能分支通过 Pull Request #1 合并到 GitHub `main`，保留完整提交历史。

## 后续工作

- 在目标 HarmonyOS 真机验收语音识别、TTS、OCR、SSE 长连接和签名安装。
- 使用轮换后的 DeepSeek Key 做小范围真实模型冒烟测试，并记录响应耗时与降级情况。
- 对接订单、收藏等尚在前端 Mock 降级的接口，补充跨设备场景测试。
- 根据真实学习数据继续优化课程推荐规则、报告指标和 RAG 教育资料。
