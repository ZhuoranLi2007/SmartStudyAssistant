# 测试记录

已验证：

- HarmonyOS API 24 `assembleHap` 构建成功。
- 后端健康检查 `/health` 返回 `status=ok`、`aiProvider=mock`。
- pytest 8项测试通过：59/60/84/85分边界、薄弱点降级、基础目标限制、注册登录、学生档案、推荐、工具调用、家庭隔离与学生绑定。
- 断网、权限拒绝、能力不支持、OCR空结果均有前端提示和加载状态恢复逻辑。

未验证：

- 本机没有MySQL，因此MySQL 8.x迁移和接口冒烟测试未执行。
- 工程没有签名配置，因此未完成真机安装。
- 语音和OCR需HarmonyOS真机最终验证；模拟器支持情况不能代替真机验收。
